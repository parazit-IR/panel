package com.parazit.panel.application.promotion.result;

public enum GiftCodeRedemptionOutcome {
    REDEEMED,
    ALREADY_REDEEMED,
    INVALID_CODE,
    EXPIRED,
    EXHAUSTED,
    USER_LIMIT_REACHED,
    WALLET_UNAVAILABLE
}
